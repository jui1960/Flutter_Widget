import 'package:flutter/material.dart';

//get -> server theke data fetxch kora, all data (https:// domain.com/api/posts), single data (https:// domain.com/api/post?id=1)
//post -> create a new post (https:// domain.com/api/post)body parameter ar maddhore details pathano hoi
//patch,put,delete


void main() {
  runApp(const MyApp());
}

class MyApp extends StatefulWidget {
  const MyApp({super.key});

  @override
  State<StatefulWidget> createState() => myAppState();
}

class myAppState extends State<MyApp> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: ,
    );
  }
}
